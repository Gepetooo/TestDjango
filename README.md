You'll need a virtual environment to properly run the website on your machine, therefore you need to have python installed.
Run these commands to create, activate and deactivate a virtual environment.
python -m venv .venv (Creates venv's folder)
./.venv/scripts/activate.ps1 (Activate venv)
deactivate (Deactivates venv)
